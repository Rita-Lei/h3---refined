function setup() { 
  createCanvas(400, 400);
} 

function draw() { 
  background(255);
  
  noFill();
  rectMode(CENTER);
  
  for(var a = 50; a <= 400; a += 100){
    for(var b = 20; b <= 80; b += 20){
      rect(a,200,b,b);
    }
  }
}
